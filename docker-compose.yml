
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: pmt
      POSTGRES_USER: pmt
      POSTGRES_PASSWORD: pmt
    ports: ["5432:5432"]
    volumes: ["pmt_pg:/var/lib/postgresql/data"]
volumes:
  pmt_pg:
